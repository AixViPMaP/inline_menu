inline_menu (ownCloud app)
==========================

Replace the default pop-down navigation menu with an inline menu on larger screens (width >=768px).

Install
-------

Copy `inline_menu` to ownCloud `apps` directory.
